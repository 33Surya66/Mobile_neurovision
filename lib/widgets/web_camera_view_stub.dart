import 'package:flutter/widgets.dart';

class WebCameraView extends StatelessWidget {
  const WebCameraView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
